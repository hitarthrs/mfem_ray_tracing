# Retired code

Local-only / retired from the public bilinear + T-spline shell workflow.

These files were previously kept in `include/`, `src/`, and `tests/` but excluded
via `.gitignore`. They are archived here so the live tree stays clean while the
code remains recoverable.

Do not link these into the main `mfem_raytracing` CMake targets unless you are
intentionally reviving an old experiment.
